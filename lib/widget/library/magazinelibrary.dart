import 'package:flutter/cupertino.dart';

class MagazineLibrary extends StatelessWidget {
  const MagazineLibrary({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('mg'),
    );
  }
}
